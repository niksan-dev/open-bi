[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "openbi"
version = "0.1.0"
description = "OpenBI Engine"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
include = ["openbi*"]